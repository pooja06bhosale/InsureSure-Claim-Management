eureka:
  client:
    register-with-eureka: true   # optional — true by default
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/ --telling where eureka running -- on google -- http://localhost:8761
    spring:
      application:
        name: auth-service --- register on eureka by this name


      values all true bsc there want to call and resister to the eureka server


      added eureka netflix client dependency

----------------------------------------------------------