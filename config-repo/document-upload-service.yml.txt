server:
  port: 8090

spring:
  application:
    name: document-upload-service

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
